<h1> <%= @goal.name %> </h1>
<h2> <%= @goal.description %></h2>
<h3>Author is going to finish this goal at <%= @goal.term %></h3>
<%= link_to 'Edit Goal', edit_user_goal_path(current_user,@goal) %>
<%= link_to 'Back to Goals', user_goals_path(current_user) %>
<%= link_to 'Delete', user_goal_path(current_user,@goal), method: :delete,
									 data: {confirm: 'Are you sure?'} %>